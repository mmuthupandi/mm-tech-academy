rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    // Allow read/write access to all collections for development purposes.
    // WARNING: For production, you should restrict these rules to only authenticated users 
    // or specific collections based on your security requirements.
    match /{document=**} {
      allow read, write: if true;
    }
  }
}
